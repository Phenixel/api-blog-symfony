nelmio_api_doc:
    documentation:
        info:
            title: Blog
            description: Ceci est la doc de l'api de mon blog
            version: 1.0.0
    areas: # to filter documented areas
        path_patterns:
            - ^/api(?!/doc$) # Accepts routes under /api except /api/doc
